var a = 0;
var b = 0;

var value = (a * a) - (2 * a * b) + (b * b);

if (value > 0) {
	alert("Bigger than zero");
}
else if (value < 0) {
	alert("Smaller than zero");
}
else {
	alert("Equals to zero");
}
